import Form from "../form/Form";
import Header from "../header/Header";
import "./style.css";

const Layout = () => {
  return (
    <div className="Layout">
      <Header></Header>
      <Form></Form>
      {/* List */}
    </div>
  );
};

export default Layout;
